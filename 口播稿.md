# VS Code C++ GDB 口播稿

> 建议时长 2-3 分钟，语速中等，配合屏幕录制演示。

---

## 开场（10秒）

你是不是受够了 VS Code 写 C++ 没法调试？装环境要折腾半天？

这个开源项目让你**一键搞定** — Windows、Linux、macOS 全支持。

## 核心问题（15秒）

平时想在 VS Code 里调试 C++，得自己装编译器、装 GDB、写 launch.json、配 tasks.json，少一步就报错。

这个项目一个脚本全自动搞定，你只需要打开 .cpp 按 F5。

## 演示（60秒）

**Windows 演示：**

先看 Windows。双击 `setup.bat`，动画效果——

脚本自己检查环境、下载 MSYS2、安装 GCC 16 和 GDB 17、配置 PATH、安装 VS Code 扩展。

第一次跑大概 3 到 8 分钟，以后重跑秒级跳过。

装完打开文件夹，打开 demo.cpp，按 F5。自动编译 → 启动 GDB 调试 → 左下角断点、变量、堆栈全都有。

程序里加上 `system("pause")` 窗口就不会闪退。

**Linux 演示：**

Linux 也一样的。`bash setup-linux.sh`，自动识别 apt 还是 dnf，装好 g++ 和 gdb，生成 .vscode 配置，按 F5 直接调试。

**macOS 演示：**

macOS 更省心。用 Xcode 自带的 LLDB，不用折腾 GDB 签名。`bash setup-mac.sh`，弹出 Xcode 安装窗口点一下，装完就能用。

## 亮点总结（20秒）

三个平台脚本都是动画版的 — 字打机 banner、进度条、庆祝动画，长得一模一样。

VS Code 检测覆盖 PATH、常见路径、注册表、全盘扫描，不管你把 VS Code 装在哪都能找到。

## 结尾（15秒）

项目地址在 GitHub，链接放简介里。MIT 协议开源，放心用。

觉得有用点个 Star，有问题提 Issue。

下个视频见。

---

## 演示流程提纲

```
1. 屏幕 → 打开 GitHub 仓库首页
2. 终端 → 双击 setup.bat，展示动画过程
3. VS Code → 打开 demo.cpp → F5 → 断点暂停
4. 左侧 → 点击变量、调用堆栈
5. 按 F10 → 逐行执行演示
6. 结束 → 回到 README
```

---

## 可配画面

| 时间 | 画面 | 口播 |
|------|------|------|
| 0:00 | GitHub 仓库 | "VS Code C++ GDB，一键调试环境" |
| 0:10 | 仓库 star/fork | "开源、MIT 协议、三平台" |
| 0:20 | 双击 setup.bat | "双击这一个文件" |
| 0:35 | 动画效果 | "自动检测、下载、安装、配置" |
| 0:50 | 安装完成 | "GDB 17 + GCC 16 就绪" |
| 1:00 | 打开 VS Code | "打开文件夹" |
| 1:10 | 打开 demo.cpp | "打开你的 .cpp 文件" |
| 1:15 | 按 F5 | "按下 F5" |
| 1:20 | 断点调试 | "断点、变量、堆栈，全都有" |
| 1:40 | Linux 演示 | "Linux 一样简单" |
| 2:00 | macOS 演示 | "macOS 用 LLDB，更省心" |
| 2:20 | 对比表格 | "三平台统一体验" |
| 2:30 | GitHub 页 | "链接在简介，点个 Star" |
